version: v2
modules:
  - path: .
deps:
  # google.api.http annotations (the canonical HTTP mapping for each RPC).
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
